export const url = process.env.NODE_ENV === "production" ? `https://wicked-ways-grocery-list.herokuapp.com` : `http://localhost:8080`;

// If you are running on a localhost started from the root file path,
// uncomment the line underneath and comment out the one above

//export const url = "http://localhost:8080"